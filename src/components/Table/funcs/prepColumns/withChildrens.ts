import { iTableProps } from "../../interfaces";
import {} from "ramda";
import { iReturn } from "./interface";

const withChildrens = (props: iTableProps): any => {
  console.log(props);

  return {};
};

export default withChildrens;
